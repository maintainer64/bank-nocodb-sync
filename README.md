# Bank NocoDB Sync

Расширение для браузера Chrome, которое автоматически синхронизирует транзакции из банковских приложений с NocoDB или
выгружает в CSV файлы.

## Поддерживаемые сервисы

- ✅ **Т-Банк**
- ✅ **Яндекс Банк**
- ✅ **Яндекс Лавка**
- ✅ **Жизнь Март**

## Функциональность

- 🔄 **Автоматическая синхронизация** транзакций в реальном времени
- 📊 **Экспорт в NocoDB** для анализа и отчетности
- 📁 **Выгрузка в CSV** для локального хранения
- 🔒 **Безопасное хранение** настроек и данных
- 🎯 **Простой интерфейс** настройки и управления

## Установка

### Способ 1: Автоматическая установка (рекомендуется)

1. Скачайте и запустите скрипт установки:

```bash
curl -s https://raw.githubusercontent.com/maintainer64/bank-nocodb-sync/main/install.sh | bash
```

Или вручную:

```bash
# Скачайте скрипт
wget https://github.com/maintainer64/bank-nocodb-sync/releases/latest/download/install.sh

# Дайте права на выполнение
chmod +x install.sh

# Запустите установку
./install.sh
```

### Способ 2: Ручная установка

1. **Скачайте последний релиз**:

```bash
# Используйте скрипт для скачивания
./install.sh
```

2. **Распакуйте расширение**:
    - Архив автоматически распакуется в `~/bank-nocodb-sync_latest/`

3. **Добавьте расширение в Chrome**:
    - Откройте Chrome и перейдите в `chrome://extensions/`
    - Включите **"Режим разработчика"** (переключатель в правом верхнем углу)
    - Нажмите **"Загрузить распакованное расширение"**
    - Выберите папку с распакованным расширением

## Настройка

### Настройка NocoDB

1. **Создайте базу данных** в NocoDB с следующими полями:
    - `date` (Date) - дата транзакции
    - `amount` (Number) - сумма
    - `description` (Text) - описание
    - `category` (Text) - категория
    - `bank` (Text) - источник (Т-Банк, Яндекс Банк и т.д.)

2. **Получите API ключ** и URL вашей NocoDB

### Настройка расширения

1. Нажмите на иконку расширения в Chrome
2. Перейдите в настройки
3. Введите:
    - **NocoDB URL** (например: `https://your-nocodb-instance.com`)
    - **API Token** (из настроек NocoDB)
    - **Database ID** (ID вашей базы)

## Использование

### Синхронизация по параметрам

1. Расширение автоматически отслеживает транзакции в поддерживаемых банковских приложениях
2. Данные синхронизируются в реальном времени при открытии банковских сервисов

### Ручной экспорт

1. Откройте нужный банковский сервис
2. Нажмите на иконку расширения
3. Выберите "Экспортировать транзакции"
4. Выберите формат (NocoDB или CSV)

## Разработка

### Требования

- Node.js 20+
- Yarn 1.x

### Установка для разработки

```bash
# Клонируйте репозиторий
git clone https://github.com/maintainer64/bank-nocodb-sync.git
cd bank-nocodb-sync

# Установите зависимости
yarn install

# Запустите в режиме разработки
yarn dev

# Соберите для production
yarn build
```

### Структура проекта

```
src/
├── index.tsx          # Основной компонент
├── background/        # Фоновые скрипты
├── content/          # Контент-скрипты для банковских сервисов
├── utils/            # Утилиты и хелперы
└── types/            # TypeScript типы
```

## Безопасность

- 🔐 **Локльное хранение** - все настройки хранятся локально в браузере
- 🌐 **Безопасные запросы** - использование HTTPS для API вызовов

## Troubleshooting

### Расширение не работает

1. Убедитесь, что расширение включено в `chrome://extensions/`
2. Проверьте настройки NocoDB (URL, API token, Table ID)
3. Откройте консоль разработчика (F12) для просмотра ошибок

### Данные не синхронизируются

1. Проверьте подключение к интернету
2. Убедитесь, что NocoDB доступен
3. Проверьте правильность Database ID

## Лицензия

Apache License

---

**Примечание**: Это расширение не связано с банками официально и работает путем анализа открытых страниц банковских
сервисов. Используйте на свой страх и риск.